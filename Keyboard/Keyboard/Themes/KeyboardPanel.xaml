<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Keyboard;assembly=">

    <Style TargetType="{x:Type local:KeyBoardPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:KeyBoardPanel}">
                    <!--<ItemsControl x:Name="Panel">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <local:PanelForKeyboard>
                                </local:PanelForKeyboard>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>-->
                        <!--<ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:KeyBoardPanel/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>-->
                        <!--</ItemsControl>-->
                    <!--</ItemsControl>-->
                    <StackPanel x:Name="PART_Stackpanel"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
